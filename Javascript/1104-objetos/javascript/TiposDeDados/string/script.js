const text = "javascript";
const frase = 'Isso é um "jogo"';

// " + " é a concatenação das frases
console.log(text + "" + frase);

let total = 0;

const compras = 30;
const imposto = 10;

total = compras + imposto;

console.log(total);

// string para número
const transformar = Number("20");
console.log(text.toUpperCase);

const nome = nome.toUpperCase();
console.log(nome.lenght);
